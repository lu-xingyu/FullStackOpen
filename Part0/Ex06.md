```mermaid
sequenceDiagram
    participant browser
    participant server

    browser->>browser: add the new note to the list, clear the input area and re-draw the updated list

    Note right of browser: Browser send information of the new note as a json format to server 
    
    browser->>server: POST https://fullstack-exampleapp.herokuapp.com/new_note_spa
    activate server
    server-->>browser: 201 Created
    deactivate server

    Note left of server: Server stores the new notes and return success
   